<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="botchat.list">
      <field name="name">botchat list</field>
      <field name="model">botchat.botchat</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="botchat.action_window">
      <field name="name">botchat window</field>
      <field name="res_model">botchat.botchat</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="botchat.action_server">
      <field name="name">botchat server</field>
      <field name="model_id" ref="model_botchat_botchat"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="botchat" id="botchat.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="botchat.menu_1" parent="botchat.menu_root"/>
    <menuitem name="Menu 2" id="botchat.menu_2" parent="botchat.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="botchat.menu_1_list" parent="botchat.menu_1"
              action="botchat.action_window"/>
    <menuitem name="Server to list" id="botchat" parent="botchat.menu_2"
              action="botchat.action_server"/>
    -->
  </data>
</odoo>